apiVersion: v1
kind: ConfigMap
metadata:
  name: app-config
  namespace: prod
data:
  NODE_ENV: "production"
  PORT: "3000"
  API_PORT: "4000"
  LOG_LEVEL: "info"
  # DB connection string is injected via Secret (from Secret Manager)
